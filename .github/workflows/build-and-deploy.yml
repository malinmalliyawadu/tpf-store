name: Build and Deploy
on:
  push:
    branches:
      - main
jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: Install and Build 🔧
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
        run: |
          cd tpf-store-frontend
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.5
        with:
          branch: gh-pages
          folder: tpf-store-frontend/dist

  # backend:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout 🛎️
  #         uses: actions/checkout@v2.3.1

  # - name: Deploy 🚀
  #   env:
  #     STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
  #   run: |
  #     cd backend
  #     cd functions
  #     npm deploy
